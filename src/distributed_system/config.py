# This is where we can put any globals or shared variables ( suggested by gemini :D )

# Network Settings
DEFAULT_HOST = "127.0.0.1"
DEFAULT_PORT = 8080

# Protocol Constants
BUFFER_SIZE = 1024
ENCODING = "utf-8"
